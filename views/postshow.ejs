<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white min-h-screen flex items-center justify-center">

    <div class="max-w-lg p-8 bg-zinc-800 rounded-lg shadow-lg space-y-4">
        <!-- Post Title -->
        <h1 class="text-2xl font-bold text-zinc-100"><%= post.title %></h1>
        
        <!-- Published By -->
        <p class="text-sm text-zinc-400">Published by: <span class="text-zinc-300 font-semibold"><%= post.user ? post.user.username : "Unknown Author" %></span></p>

        <!-- Post Content -->
        <div class="mt-4 text-zinc-200">
            <p><%= post.content %></p>
        </div>

        <!-- Go Back Link -->
        <div class="mt-6">
            <a href="/feed" class="text-zinc-400 hover:text-zinc-200 underline">Go Back</a>
        </div>
    </div>

</body>
</html>
